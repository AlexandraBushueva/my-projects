#Урок 1
**Списки, метод diff, индексация**

import pandas

# список campaign_weeks из 10 чисел
campaign_weeks = [7, 9, 13, 15, 17, 19, 29, 31, 33, 45]

# читаем файл и записываем в переменную
data = pandas.read_csv('app_stats.csv')

# сохраняем столбец с установками в installs
installs = data['installs']
# индексация: формируем новый столбец, в котором только
# элементы из installs с указанными в campaign_weeks индексами
filtered_data = installs[campaign_weeks]
print(filtered_data)
